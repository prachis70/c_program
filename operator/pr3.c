#include<stdio.h>
main()
{
	int x,y;
	int sum;
	printf("enter the number:");
	scanf("%d",&x);
	printf("enter the number:");
	scanf("%d",&x);
	sum=((x*x*x)+(3*x*y)*(x+y))+(y*y*y);
	printf("ans is%d=",sum);
	
	
}