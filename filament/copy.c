#include<stdio.h>
#include<string.h>
main()
{
	char a[30];
	char b[30];
    int	ans=strcpy(a,b);
	puts("Enter the any value.");
	gets(a);
	puts("Enter the any value.");
	gets(b);
	printf("%s\n",b);
	printf("%s",a);
	
	
}
